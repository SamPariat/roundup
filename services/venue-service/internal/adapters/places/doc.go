// Package places provides a Google Maps implementation of domain.PlaceProvider.
// To swap the provider, write a new adapter in this package and change the single
// constructor call in cmd/main.go — the domain and application layers are untouched.
package places
