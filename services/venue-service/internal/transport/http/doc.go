// Package http contains the Fiber HTTP handlers and router for the venue-service.
// Handlers parse HTTP requests, delegate to use cases, and write JSON responses
// using the standard Response envelope defined in the dtos package.
package http
